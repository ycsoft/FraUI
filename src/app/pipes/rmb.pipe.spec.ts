/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RmbPipe } from './rmb.pipe';

describe('RmbPipe', () => {
  it('create an instance', () => {
    const pipe = new RmbPipe();
    expect(pipe).toBeTruthy();
  });
});
